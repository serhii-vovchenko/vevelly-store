import { Container } from '../Container/Container';
import { Topline } from './Topline';

const Header = () => {
	return (
		<header>
				<Topline />
				Header
		</header>
	);
};

export default Header;
